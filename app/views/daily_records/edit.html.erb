<% content_for :title, t("daily_records.edit.title") %>

<section class="rounded-3xl bg-white px-8 py-10 shadow-sm">
  <h1 class="mt-3 text-3xl font-bold leading-tight"><%= t("daily_records.edit.title") %></h1>
  <%= render "form", daily_record: @daily_record %>
</section>

<%= render "shared/back_home_button", path: mypage_path, label: t("common.back_to_mypage") %>
